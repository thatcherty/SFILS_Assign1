{
  "collMod": "notification_pref",
  "validator": {
    "$jsonSchema": {
      "bsonType": "object",
      "required": [ "notif_pref_code", "notif_pref" ],
      "properties": {
        "notif_pref_code": {
          "bsonType": "string",
          "description": "must be a string and is required"
        },
        "notif_pref": {
          "bsonType": "string",
          "description": "must be a string and is required"
        }
      }
    }
  }
}
