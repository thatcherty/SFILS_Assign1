{
  "collMod": "home_libraries",
  "validator": {
    "$jsonSchema": {
      "bsonType": "object",
      "required": [ "home_library_code", "home_library" ],
      "properties": {
        "home_library_code": {
          "bsonType": "string",
          "description": "must be a string and is required"
        },
        "home_library": {
          "bsonType": "string",
          "description": "must be a string and is required"
        }
      }
    }
  }
}
